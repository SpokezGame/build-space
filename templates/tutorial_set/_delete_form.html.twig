<form method="post" action="{{ path('app_tutorial_set_delete', {'id': tutorial_set.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tutorial_set.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
