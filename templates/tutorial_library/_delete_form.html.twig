<form method="post" action="{{ path('app_tutorial_library_delete', {'id': tutorial_library.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tutorial_library.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
